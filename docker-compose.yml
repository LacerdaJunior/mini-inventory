version: "3.8"

services:
  api:
    build: .
    container_name: inventory_api
    ports:
      - "3333:3333"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    env_file:
      - .env
